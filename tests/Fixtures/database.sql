PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE import_job (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, entity_class VARCHAR(255) NOT NULL, executed DATETIME NOT NULL, runtime INTEGER DEFAULT NULL, memory INTEGER DEFAULT NULL, records INTEGER NOT NULL, result VARCHAR(255) DEFAULT NULL);
CREATE TABLE entity_fixture (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, int_field INTEGER DEFAULT NULL, float_field DOUBLE PRECISION DEFAULT NULL, string_field VARCHAR(255) DEFAULT NULL, text_field CLOB DEFAULT NULL);
DELETE FROM sqlite_sequence;
COMMIT;
